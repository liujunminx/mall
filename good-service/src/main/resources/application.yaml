server:
  port: 5003
  servlet:
    context-path: /good

spring:
  datasource:
    username: root
    password: r00tme
    url: jdbc:mysql://localhost:3306/good?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: good-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka